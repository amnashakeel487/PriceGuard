# Presentation dependency injection providers will be defined here.
# For example, injecting SQLAlchemy repositories or Use Case executors.

# Example:
# def get_user_repository(db: Session = Depends(get_db)) -> IUserRepository:
#     return SQLAlchemyUserRepository(db)
